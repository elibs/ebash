name: Publish Docs

on:
  push:
    branches: [ master ]

jobs:

  PublishDocs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install Dependencies
        run: sudo bin/ebash-install-deps

      - name: Publish Docs
        run: bin/edoc --publish
